# 🚀 Инструкция по установке StratCraft Launcher v1.1.9

## ✅ Что исправлено в этой версии?

### Главные улучшения:
1. **Автоматический поиск Java** — лаунчер сам найдет Java на вашем компьютере
2. **Нет зависания при установке** — распаковка клиента происходит плавно
3. **Исправлен краш "Java Virtual Machine"** — автоматическая настройка памяти
4. **Клиент больше не скачивается заново** — при повторном запуске использует установленную версию
5. **Красивый прогресс-бар** — показывает скорость загрузки и время до завершения

## 📥 Как установить?

### Шаг 1: Скачайте установщик
1. Перейдите на страницу релиза: https://github.com/DiEgo22912/StratCraftLauncher/releases/tag/v1.1.9
2. Скачайте файл `StratCraftLauncher-setup-1.1.9.exe`

### Шаг 2: Запустите установщик
1. Откройте скачанный файл `StratCraftLauncher-setup-1.1.9.exe`
2. **Появится предупреждение Windows**: "Windows защитила ваш компьютер"
   
   Это нормально! Нажмите:
   ```
   Подробнее → Выполнить в любом случае
   ```
   
   > ℹ️ **Почему появляется предупреждение?**  
   > Это новое приложение без дорогого сертификата подписи ($300-500/год).  
   > После накопления репутации предупреждение исчезнет.

3. Выберите папку установки (или оставьте по умолчанию)
4. Нажмите "Установить"

### Шаг 3: Первый запуск
1. После установки запустите лаунчер из меню "Пуск" или с рабочего стола
2. Войдите в аккаунт или создайте новый
3. При первом запуске клиент скачается автоматически (показывается прогресс-бар)

## ☕ Про Java

### Если у вас уже установлена Java 17+:
✅ Лаунчер найдет её автоматически — ничего не нужно делать!

### Если Java не установлена:
Вариант 1 (рекомендуется): Установите **Eclipse Temurin**
1. Перейдите на https://adoptium.net/
2. Скачайте **JDK 17 (LTS)**
3. Установите как обычную программу

Вариант 2: Используйте встроенную Java из клиента
- Если в клиенте есть папка `runtime/` или `jre/`, лаунчер её найдет автоматически

### Проверка установленной Java:
Откройте **Командную строку** (Win+R → cmd) и введите:
```cmd
java -version
```

Должно появиться что-то вроде:
```
openjdk version "17.0.9" 2023-10-17
```

## 🔄 Обновление со старой версии

Если у вас уже установлен лаунчер:
1. Просто запустите новый установщик
2. Он автоматически обновит лаунчер
3. Все настройки, аккаунты и клиенты сохранятся

Или используйте **автообновление**:
- При запуске старого лаунчера появится кнопка "Скачать обновление"
- Нажмите её → "Установить и перезапустить"

## ⚙️ Системные требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| **ОС** | Windows 7 SP1 (64-bit) | Windows 10/11 |
| **Java** | 17 или выше | 17 LTS |
| **RAM** | 4 ГБ | 8 ГБ |
| **Место на диске** | 1 ГБ | 2 ГБ |
| **Интернет** | Для загрузки клиента | — |

## ❓ Частые вопросы

### "Windows защитила ваш компьютер" — это вирус?
**Нет!** Это стандартное предупреждение SmartScreen для новых приложений.  
Код полностью открыт на GitHub, можете проверить сами.

### Как убрать предупреждение SmartScreen?
Полностью убрать можно только купив сертификат подписи (~$300-500/год).  
Мы добавили инструкции в [CODE_SIGNING.md](CODE_SIGNING.md).

### Клиент не запускается: "Failed to create Java Virtual Machine"
**Исправлено в v1.1.9!** Обновите лаунчер.  
Если всё равно не работает — убедитесь что Java 17 установлена.

### Лаунчер зависает при установке клиента
**Исправлено в v1.1.9!** Теперь распаковка асинхронная.

### Клиент скачивается заново каждый раз
**Исправлено в v1.1.9!** Теперь проверяется правильно.

### Где хранятся данные лаунчера?
```
%APPDATA%\StratCraftLauncher\data\
```

Там находятся:
- `clients/` — установленные версии клиента
- `users.json` — сохраненные аккаунты
- `launcher-settings.json` — настройки

## 🐛 Нашли баг?

Создайте Issue на GitHub:  
https://github.com/DiEgo22912/StratCraftLauncher/issues

Укажите:
- Версию лаунчера (1.1.9)
- Версию Windows
- Текст ошибки (если есть)
- Что делали перед ошибкой

## 📞 Поддержка

- **GitHub Issues**: https://github.com/DiEgo22912/StratCraftLauncher/issues
- **Автор**: yukinch
- **Discord**: [Ссылка на Discord сервер, если есть]

---

**Спасибо за использование StratCraft Launcher! 🎮**
